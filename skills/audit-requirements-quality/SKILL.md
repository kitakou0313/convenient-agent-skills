---
name: audit-requirements-quality
description: IEEE Std 830が定義するSRS(Software Requirements Specification)の品質特性（Correct/Unambiguous/Complete/Consistent/Ranked for importance and stability/Verifiable/Modifiable/Traceable）の考え方を、ソフトウェアに限らずあらゆる要求（ビジネス要件・法務/契約条項・組織/業務規程・個人の計画等）に一般化した基準で評価し、改善点をフィードバックする。要件が2件以上渡された場合は要件セット全体で判定する特性（Complete/Consistent/Ranked）も評価し、単一要件のみの場合はそれらを「判定不可」として明示する。要件テキストの範囲内でのみ判定し、外部文脈（上位方針・優先度・トレーサビリティ情報等）が必要な項目は判定不可とその根拠・必要情報を明記する。問題のある要件には指摘点を統合した書き直し案を提示するが、確定的な置き換えとしては提示しない。評価と改善案の提示のみを行い、以降どう反映するかはユーザーに委ねる。「この要件の品質を評価して」「要件が曖昧じゃないか確認して」「要件定義書をレビューして」「この要求仕様は完全か確認して」「要件の改善点を教えて」といった依頼で使用する。
compatibility: 要件をファイル/Issue・チケットURLで指定された場合、ファイル読み取りやWeb取得・`gh`コマンド等が使えれば動的に使用する（無くても、ユーザーへの内容貼り付け依頼で動作する）
---

# Audit Requirements Quality — 要求を品質特性で監査する（領域非依存）

渡された要件（ソフトウェアに限らず、ビジネス要件・法務/契約条項・組織/業務規程・個人の計画など、あらゆる
「要求」を対象とする）を、IEEE Std 830が定義するSRSの品質特性の考え方をソフトウェア以外にも一般化した
基準で評価し、改善点をフィードバックするスキル。要件テキストの範囲内で判定できることと判定できないことを
明確に切り分け、判定できない場合は何を確認すればよいかを示す。問題が見つかった要件には統合した書き直し案
を提示するが、それをどう扱うか（採用する・別の直し方をする・保留する等）はユーザーの判断に委ねる。

## 全体の流れ

1. **要件の受け取り**（ユーザーとの対話）— 評価対象の要件を確認する
2. **要件の分割・粒度判定**（Agentが実施）— 単一要件か複数要件（要件セット）かを判定し、評価する特性を決める
3. **特性ごとの評価**（Agentが実施）— 8特性それぞれについて判定する
4. **改善案の作成**（Agentが実施）— 問題のある要件について統合した書き直し案を作る
5. **レポート生成と保存**（Agentが実施）— Markdownにまとめ、ローカルに保存する

## 要求品質の8つの特性

このスキルは、IEEE Std 830-1998が定義するSRS（Software Requirements Specification）の品質特性の考え方
を、ソフトウェアという主語を外し、あらゆる「要求」に適用できるよう一般化したものである。◎は単一要件でも
評価可能、要は要件が2件以上必要（要件セット全体の性質）であることを示す。

1. **Correct（正確）**◎ — 要件が実際に対象が満たすべき事柄を記述しているか
2. **Unambiguous（曖昧でない）**◎ — 一つの解釈しかできない記述になっているか
3. **Complete（完全）**要 — 想定されるケース・状況のクラスに対する扱いが要件セット内で規定されているか、
   未確定事項（TBD等）が残っていないか
4. **Consistent（一貫している）**要 — 要件間に論理的矛盾や用語の不整合がないか
5. **Ranked for importance and/or stability（重要度・安定度による優先順位付け）**要 —
   各要件に重要度または変更されにくさの情報が付与されているか
6. **Verifiable（検証可能）**◎ — 満たしているかどうかを客観的に確認できる基準で記述されているか
7. **Modifiable（変更可能）**◎/要 — 1要件が1つの関心事に限定されているか（単一要件でも判定可）、
   要件間の重複がないか（要件セットが必要）
8. **Traceable（追跡可能）**◎ — 一意なID・上位要求への参照など、追跡のための情報が付与されているか

## Phase 1: 要件の受け取り

- ユーザーから評価対象の要件を受け取る。チャット上の自由記述、またはファイルパス/Issue・チケットURLの
  指定を受け付ける
  - ファイルパス/URLが指定された場合、利用可能なツール（ファイル読み取り、Web取得、`gh`コマンド等）で
    内容の取得を試みる。取得できない場合はその旨を伝え、ユーザーに内容の貼り付けを依頼する
- 要件と呼べる記述が1件も読み取れない場合は、その旨を伝え対象の再指定を依頼する

## Phase 2: 要件の分割・粒度判定

- 入力を、独立して評価できる「要件」単位（番号付きリストの各項目、独立した文、箇条書きの各項目等）に分割する
- 分割方針が曖昧な場合（1文に複数の要求が混在している等）は、Agentが暫定的に分割し、その分割結果を
  Phase 5のレポート冒頭で明示する（ユーザーの意図と異なる場合に再指定できるようにするため）
- 分割後の要件数に応じて、Phase 3で評価する特性の範囲を決める
  - **1件のみ**: Correct/Unambiguous/Verifiable/Modifiable（1関心事の観点のみ）/Traceableを評価する。
    Complete/Consistent/Rankedおよび Modifiable の重複観点は「判定不可（要件が1件のみのため、2件以上の
    要件セットが必要）」とする
  - **2件以上**: 8特性すべてを評価する

## Phase 3: 特性ごとの評価

各要件（または要件セット全体で判定する特性についてはセット全体）について、以下の基準で判定する。
判定は **◯（満たす）/△（部分的に満たす・軽微な問題）/✗（満たさない）/対象外（要件数が足りない）/
判定不可（本文だけでは判断できない）** の5段階とし、単一のスコアには集約しない。判定不可の場合は、
判定するために何の情報が必要か（上位方針文書、関係者への確認、優先度情報等）を明記する。

### 3-1. Correct（正確）

- 要件が実際のステークホルダー（利用者・関係部署・契約相手等）の必要と一致するかどうかは、要件本文だけ
  からは判定できない。この観点は常に**判定不可**とし、必要な情報として「上位方針文書または関係者への
  確認」を明記する
- 一方で、本文内で確認できる代理的な問題（自己矛盾する記述、要件セット内の他の記述と矛盾する事実、
  両立し得ない条件・数値の組み合わせ）は検出し、◯/△/✗で判定する。問題がなければ「本文内で確認できる
  範囲では問題なし。実際のニーズとの一致は別途確認が必要」と明記する

### 3-2. Unambiguous（曖昧でない）

- 複数の解釈が可能な語（主観的な形容詞・副詞「速やかに」「適切に」「十分な」「わかりやすい」等、
  単位・基準のない数値、「等」で終わる非網羅的な列挙）を検出する
- 定義なく使われている専門用語・独自用語があれば指摘する
- 解釈が割れる箇所は、実際に割れる2パターン以上の解釈例を挙げて具体的に示す

### 3-3. Complete（完全）

- 要件が2件以上の場合のみ評価する（1件のみの場合は対象外）
- 未確定事項（TBD/TODO/仮の値等）が残っていないかを確認する
- 要件文中で言及されているケース・状況のクラスに対して、想定される扱い（通常時だけでなく例外時・
  境界的なケースへの扱い）が要件セット内に規定されているかを確認する
- ある要件が暗に前提としている別の要件（例:「承認済みであること」を前提とする要件に対応する承認プロセス
  の要件）が要件セット内に存在するかを確認する
- 要件セットそのものが上位のスコープ全体を余さず覆っているか（そもそも抜けている領域があるか）は
  本文だけでは判定できないため、この観点は**判定不可**とし、必要な情報として「上位スコープ文書・方針書」
  を明記する

### 3-4. Consistent（一貫している）

- 要件が2件以上の場合のみ評価する（1件のみの場合は対象外）
- 同じ対象について異なる要件が矛盾する扱いを要求していないかを確認する
- 同じ概念に異なる用語が使われている、または異なる概念に同じ用語が使われていないかを確認する

### 3-5. Ranked for importance and/or stability（優先順位付け）

- 要件が2件以上の場合のみ評価する（1件のみの場合は対象外）
- 各要件に重要度または安定度（変更されにくさ）を示す情報（優先度ラベル、MoSCoW、Must/Should/Could等）
  が付与されているかを確認する
- 付与されていない場合は「優先順位・安定度の情報が付与されていない」と指摘するのみに留め、Agent自身が
  代わりに優先順位を提案することはしない（このスキルは評価とフィードバックのみを行う）

### 3-6. Verifiable（検証可能）

- 各要件が、客観的に確認可能な基準（数値・条件・観測可能な状態や成果物）で記述されているかを確認する
- 「〜であるべき」「適切であること」「わかりやすいこと」等、客観的には確認できない主観的な表現を検出する

### 3-7. Modifiable（変更可能）

- 1つの要件文に複数の独立した関心事が混在していないか（1要件1関心事の原則）を確認する。これは単一
  要件でも判定できる
- 要件が2件以上の場合は、同じ制約・内容が複数箇所に重複して書かれていないかも確認する（1件のみの
  場合はこの観点のみ対象外とする）

### 3-8. Traceable（追跡可能）

- 各要件に一意なID・番号が付与されているか、上位要求への参照があるかを確認する
- 付与されていない場合は「追跡可能性の情報が付与されていない」と指摘し、必要な情報として「ID体系また
  は上位要求文書への参照」を明記する

## Phase 4: 改善案の作成

Phase 3で△または✗と判定された要件について、指摘された問題点をすべて反映した**1つの統合された
書き直し案**を作成する。判定不可だった観点（Correctの正確な一致確認、Complete/Ranked/Traceableの
外部情報が必要な部分等）は書き直しの対象にはせず、Phase 5で「判定に必要な追加情報」として別途扱う。

書き直し案には、どの修正がどの特性の指摘に対応しているかを説明文で明記する。書き直し案はあくまで
「案」であり、確定的な置き換えとして提示しない旨を明記する。

## Phase 5: レポート生成と保存

以下の構成でMarkdownレポートを生成する:

1. **概要**: 対象とした要件の総数、単一/要件セットのどちらとして扱ったか、Phase 2での分割結果
2. **要件×特性 判定表**: 行を各要件（Complete/Consistent/Rankedは「要件セット全体」の行として別途
   追加）、列を8特性とし、セルに◯/△/✗/対象外/判定不可の記号を入れた表
3. **詳細**: 要件ごとに、△・✗・判定不可と判定した特性それぞれについて具体的な理由（本文中の該当箇所の
   引用を含む）を記載する
4. **改善案**: Phase 4で作成した統合済みの書き直し案を、対象要件・対応する指摘とともに提示する
5. **判定に必要な追加情報**: 判定不可だった項目と、それぞれ何があれば判定できるかの一覧

生成したMarkdownを`scripts/save_report.sh`に標準入力で渡して保存する（対象名の推定、タイムスタンプ
付与、同一対象への再実行時の上書き回避はスクリプトが行う）。スクリプトは
`{"status":"ok","path":"..."}`または`{"status":"error","reason":"...","message":"..."}`形式の
JSONを1行で標準出力するので、`status`が`ok`ならその`path`を保存先としてユーザーに報告する。

## エッジケース対応

- **要件が1件のみ**: Complete/Consistent/Rankedおよび Modifiable の重複観点を対象外とし、それらの
  評価には2件以上の要件セットが必要である旨をレポートに明記する
- **入力が"shall"文でない宣言的記述（ユーザーストーリー形式、契約条項、業務規程の箇条書き、個人の
  目標設定等）**: そのまま1件（または分割後の各項目）の要件として扱い、通常の特性評価を適用する
- **要件の分割方針が曖昧**（1文に複数の要求が混在している等）: Agentが暫定的に分割し、その分割結果を
  レポート冒頭に明示する（Phase 2参照）
- **ファイル/URL指定だが取得に失敗する**: その旨を伝え、内容をチャットに貼り付けてもらう
- **要件と呼べる記述が1件も読み取れない**: その旨を伝え、対象の再指定を依頼する（Phase 1参照）
- **判定不可だった項目についてユーザーが追加情報（上位方針文書、優先度、トレーサビリティ情報等）を
  提供した場合**: その情報をもとに該当項目のみ再評価し、判定を確定させる
- **`scripts/save_report.sh`が`status: "error"`を返す（保存先に書き込めない等）**: `message`の内容を
  伝え、代替の保存先をユーザーに確認する
- **shellスクリプトを実行できない環境**: `scripts/save_report.sh`を使わず、生成したMarkdownをそのまま
  チャット上に提示し、保存はユーザーに委ねる
